# /Users/thuan/.config/tmuxinator/prac.yml
# Tmux template for practice or free coding

name: prac
root: ~/p/practice

windows:
  - ide: cd ~/.dotfiles && nvim
  - ex-1: cd ~/.dotfiles
  - ex-2: cd ~/.dotfiles
