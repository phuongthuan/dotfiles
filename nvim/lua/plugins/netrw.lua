local g = vim.g

require("netrw").setup({
	-- Put your configuration here, or leave the object empty to take the default
	-- configuration.
	icons = {
		symlink = "",
		directory = "",
		file = "",
	},
	use_devicons = true,
	mappings = {},
})
